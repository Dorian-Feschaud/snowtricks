
{% block title %}Utilisateurs{% endblock %}

{% block body %}

<div class="">
    <h1>Utilisateurs</h1>
    <div class="">
        {% for user in users %}
            <a href="{{ path('app_user_show', {id: user.id}) }}"><h3>{{ user.email }}</h3></a>
            <p>{{ user.firstname }}</p>
            <p>{{ user.lastname }}</p>
        {% endfor %}
    </div>
</div>

{% endblock %}


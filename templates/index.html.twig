{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}

<div class="">
    <h1>Accueil</h1>
    <div>
        <a href="{{ path('app_trick') }}">Tricks</a>
        <a href="{{ path('app_user') }}">Users</a>
    </div>
</div>

{% endblock %}
